import React from 'react';
import ReactDOM from 'react-dom';
import ButtonApp from './components/buttonComponent/ButtonApp';

ReactDOM.render(
  <React.StrictMode>
    <ButtonApp />
  </React.StrictMode>,
  document.getElementById('root')
);
